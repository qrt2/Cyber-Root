#!/usr/bin/env bash

# Cores
CYAN='\033[0;36m'
GREEN='\033[0;32m'
NC='\033[0m'

echo -e "${CYAN}[*] Configurando Ambiente Cyber-Root...${NC}"

# Limpa aliases antigos de sudo para evitar conflitos
if grep -q "alias sudo=" ~/.bashrc; then
    sed -i '/alias sudo=/d' ~/.bashrc
fi

# Adiciona as funções ao .bashrc se não existirem
if ! grep -q "Cyber-Root" ~/.bashrc; then
    cat <<'EOF' >> ~/.bashrc

# --- Início Cyber-Root ---
# Funções para Shell Root e Sudo
sudo() {
    if [ $# -eq 0 ]; then
        su -i
    else
        su -i -c "$*"
    fi
}

root() {
    local MY_PS1='\[\e[1;31m\]Cyber-Root\[\e[0m\]:\[\e[1;32m\]\w\[\e[0m\]# '
    su -i -c "export PATH=$PATH; export HOME=$HOME; export TERM=$TERM; export PS1='$MY_PS1'; exec bash --norc --noprofile"
}
# --- Fim Cyber-Root ---
EOF
fi

# Recarrega o .bashrc
source ~/.bashrc

echo -e "${GREEN}[+] Configuração concluída com sucesso!${NC}"
echo -e "--------------------------------------"
echo -e "${CYAN}sudo <comando>${NC}  - Executa comandos como root"
echo -e "${CYAN}root${NC}           - Entra no Shell Root interativo"
echo -e "--------------------------------------"

